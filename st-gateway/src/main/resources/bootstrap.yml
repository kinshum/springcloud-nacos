server:
  port: 80

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
    loadbalancer:
      retry:
        enabled: true

# 聚合文档配置
st:
  document:
    resources:
      - name: 授权模块
        location: /st-auth
      - name: 工作台模块
        location: /st-desk
      - name: 系统模块
        location: /st-system


